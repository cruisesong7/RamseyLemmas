\chapter{Ramsey Theory}

\begin{definition}[$(x,y)$-graphs]
    \label{def:isXYGraph}
    \lean{SimpleGraph.isXYGraph}
    \leanok
    % \uses{def:indepNum, def:cliqueNum}
    $G$ is an $(x,y)$-graph if $x > C(G)$ and $y > I(G)$.
\end{definition}

\begin{definition}[Ramsey Number (Graver's version)]
    \label{def:RamseyOld}
    \lean{SimpleGraph.RamseyOld}
    \leanok
    \uses{def:isXYGraph}
    $$
    R(x,y) = \max \{ n \in \mathbb{N} \mid \exists G \text{, } G \text{ is an} (x, y)\text{-graph } \land \text{ } |V(G)| = n \}
    $$
\end{definition}

\begin{theorem}[Graver's Lemma 1]
    \label{thm:Lemma₁}
    \lean{SimpleGraph.Lemma₁}
    \leanok
    \uses{def:isXYGraph}
    $G$ is an $(x,y)$-graph if and only if its complement $G^c$ is a $(y,x)$-graph.
\end{theorem}
\begin{proof}
    \leanok
\end{proof}

\begin{theorem}[noXYGraphIffRamseyGraphProp]
    \label{thm:noXYGraphIffRamseyGraphProp}
    \lean{SimpleGraph.noXYGraphIffRamseyGraphProp}
    \leanok
    \uses{def:isXYGraph, l:exists_isNClique_of_le_cliqueNum, l:induce_compl, l:exists_isNIndset_of_le_indepNum}
\end{theorem}
\begin{proof}
    \leanok
\end{proof}

\begin{lemma}[isXYGraph\_bddAbove]
    \label{l:isXYGraph_bddAbove}
    \lean{SimpleGraph.isXYGraph_bddAbove}
    \leanok
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{lemma}[cardLERamseyOld]
    \label{l:cardLERamseyOld}
    \lean{SimpleGraph.cardLERamseyOld}
    \leanok
    \uses{def:isXYGraph, l:isXYGraph_bddAbove, l:cliqueNum, l:indepNum}
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{lemma}[R3y\_neighbor\_Ind]
    \label{l:R3y_neighbor_Ind}
    \lean{SimpleGraph.R3y_neighbor_Ind}
    \leanok
    \uses{def:isXYGraph, l:cliqueNum_lt_iff_cliqueFree}
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{theorem}[GraphRamsey2RamseyOld]
    \label{thm:GraphRamsey2RamseyOld}
    \lean{SimpleGraph.GraphRamsey2RamseyOld}
    \leanok
    \uses{def:isXYGraph, def:RamseyOld, thm:noXYGraphIffRamseyGraphProp, l:cliqueNum, l:indepNum, l:isXYGraph_bddAbove}
\end{theorem}
\begin{proof}
    \leanok
\end{proof}

\begin{theorem}[RamseyOld\_2]
    \label{thm:RamseyOld₂}
    \lean{SimpleGraph.RamseyOld₂}
    \leanok
    \uses{def:RamseyOld, thm:GraphRamsey2RamseyOld}
\end{theorem}
\begin{proof}
    \leanok
\end{proof}

\begin{definition}[$v_i$]
    \label{abb:vᵢ}
    \lean{SimpleGraph.vᵢ}
    \leanok
    \uses{def: RamseyOld}
\end{definition}

\begin{definition}[$s_i$]
    \label{abb:sᵢ}
    \lean{SimpleGraph.sᵢ}
    \leanok
\end{definition}

\begin{definition}[$t_i$]
    \label{abb:tᵢ}
    \lean{SimpleGraph.tᵢ}
    \leanok
\end{definition}

\begin{definition}[$σ(G)$]
    \label{def:σ_G}
    \lean{SimpleGraph.σ_G}
    \leanok
    \uses{def:RamseyOld}
\end{definition}

\begin{lemma}[sum\_degree\_eq\_sum\_over\_degrees]
    \label{l:sum_degree_eq_sum_over_degrees}
    \lean{SimpleGraph.sum_degree_eq_sum_over_degrees}
    \leanok
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{lemma}[num\_of\_vertices\_eq\_sum\_over\_degrees]
    \label{l:num_of_vertices_eq_sum_over_degrees}
    \lean{SimpleGraph.num_of_vertices_eq_sum_over_degrees}
    \leanok
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{definition}[$H_1$]
    \label{def:H₁}
    \lean{SimpleGraph.H₁}
    \leanok
\end{definition}

\begin{definition}[$H_2$]
    \label{def:H₂}
    \lean{SimpleGraph.H₂}
    \leanok
\end{definition}

\begin{definition}[$e_1$]
    \label{abb:e₁}
    \lean{SimpleGraph.e₁}
    \leanok
    \uses{def:H₁}
\end{definition}

\begin{definition}[$e_2$]
    \label{abb:e₂}
    \lean{SimpleGraph.e₂}
    \leanok
    \uses{def:H₂}
\end{definition}

\begin{definition}[$H_1 H_2$ isomorphic]
    \label{def:H₁₂_iso}
    \lean{SimpleGraph.H₁₂_iso}
    \leanok
    \uses{def:H₁, def:H₂}
\end{definition}

\begin{lemma}[$H_1$\_eq\_bot\_of\_3y]
    \label{l:H₁_eq_bot_of_3y}
    \lean{SimpleGraph.H₁_eq_bot_of_3y}
    \leanok
    \uses{def:H₁, def:isXYGraph}
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{lemma}[$H_1$\_cliqueNum\_lt]
    \label{l:H₁_cliqueNum_lt}
    \lean{SimpleGraph.H₁_cliqueNum_lt}
    \leanok
    \uses{def:H₁, l:cliqueNum_pos}
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{theorem}[Graver Lemma 2]
    \label{thm:Lemma₂}
    \lean{SimpleGraph.Lemma₂}
    \leanok
    \uses{def:isXYGraph, def:RamseyOld, def:H₁, l:indepNum_mono, l:H₁_cliqueNum_lt, l:cliqueNum_pos, l:indepNum_pos, thm:Lemma₁}
    For any $(x,y,n)$-graph $G$, every vertex $v$ in $G$ satisfies 
    \[(n - 1) - R(x,y - 1) \leq deg(v) \leq R(x - 1, y)\].
\end{theorem}
\begin{proof}
    \leanok
\end{proof}

\begin{lemma}[G\_degreeCount\_eq]
    \label{l:G_degreeCount_eq}
    \lean{SimpleGraph.G_degreeCount_eq}
    \leanok
    \uses{def:isXYGraph, def:RamseyOld, def:σ_G, l:sum_degree_eq_sum_over_degrees, thm:Lemma₂, abb:sᵢ, abb:vᵢ}
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{lemma}[G\_vertCount\_eq]
    \label{l:G_vertCount_eq}
    \lean{SimpleGraph.G_vertCount_eq}
    \leanok
    \uses{def:isXYGraph, def:RamseyOld, def:σ_G, l:num_of_vertices_eq_sum_over_degrees, thm:Lemma₂, abb:sᵢ, abb:vᵢ}
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{lemma}[$H_1$\_degreeCount\_eq]
    \label{l:H₁_degreeCount_eq}
    \lean{SimpleGraph.H₁_degreeCount_eq}
    \leanok
    \uses{def:isXYGraph, def:RamseyOld, def:σ_G, thm:Lemma₂, abb:tᵢ, abb:vᵢ}
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{lemma}[$H_1$\_vertCount\_eq]
    \label{l:H₁_vertCount_eq}
    \lean{SimpleGraph.H₁_vertCount_eq}
    \leanok
    \uses{def:isXYGraph, def:RamseyOld, def:σ_G, thm:Lemma₂, abb:vᵢ, abb;tᵢ}
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{theorem}[Graver's Prop 1]
    \label{thm:Prop₁}
    \lean{SimpleGraph.Prop₁}
    \leanok
    \uses{def:isXYGraph, def:RamseyOld, def:σ_G, thm:Lemma₂}
\end{theorem}
\begin{proof}
    \leanok
\end{proof}

\begin{lemma}[$v_i$\_deg\_swap]
    \label{l:vᵢ_deg_swap}
    \lean{SimpleGraph.vᵢ_deg_swap}
    \leanok
    \uses{def:isXYGraph, def:σ_G, thm:Lemma₂,abb:vᵢ}
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{theorem}[Graver's Prop 2]
    \label{thm:Prop₂}
    \lean{SimpleGraph.Prop₂}
    \leanok
    \uses{def:isXYGraph, def:RamseyOld, def:σ_G, def:H₁, def:H₂,abb:e₁,abb:e₂,abb:sᵢ,abb:vᵢ,abb:tᵢ, inst:decBex, def:toOrderedPair, l:toOrderedPair_repr, l:exists_mem_pair, l:toOrderedPair_inj, l:toOrderedPair_IsDiag_of_eq}
\end{theorem}
\begin{proof}
    \leanok
\end{proof}

\begin{definition}[$e$]
    \label{def:e}
    \lean{SimpleGraph.e}
    \leanok
    \uses{def:isXYGraph}
\end{definition}

\begin{lemma}[exy0]
    \label{l:exy0}
    \lean{SimpleGraph.exy0}
    \leanok
    \uses{def:isXYGraph, def:e, l:cliqueNum_pos, l:isDiag_of_subsingleton}
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{lemma}[$e_2$\_ge\_e]
    \label{l:e₂_ge_e}
    \lean{SimpleGraph.e₂_ge_e}
    \leanok
    \uses{def:isXYGraph, def:e, abb:e₂, l:cliqueNum, l:indepNum, l:cliqueNum_mono, l:indepNum_pos, l:H₁_cliqueNum_lt, l:exy0}
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{theorem}[Graver's Prop 4]
    \label{thm:Prop₄}
    \lean{SimpleGraph.Prop₄}
    \leanok
    \uses{def:isXYGraph, def:RamseyOld, def:σ_G, def:e, abb:vᵢ, abb:sᵢ, abb:e₂, thm:Lemma₂, def:H₂, thm:RamseyOld₂, l:vᵢ_deg_swap}
\end{theorem}
\begin{proof}
    \leanok
\end{proof}