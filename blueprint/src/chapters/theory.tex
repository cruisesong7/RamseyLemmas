\chapter{Ramsey Theory}

\begin{definition}[$(x,y)$-graphs]
    \label{def:xyGraph}
    \lean{SimpleGraph.isXYGraph}
    \leanok
    % \uses{def:indepNum, def:cliqueNum}
    $G$ is an $(x,y)$-graph if $x > C(G)$ and $y > I(G)$.
\end{definition}

\begin{definition}[Ramsey Number (Graver's version)]
    \label{def:ramseynumber}
    \lean{SimpleGraph.RamseyOld}
    \leanok
    \uses{def:xyGraph}
    $$
    R(x,y) = \max \{ n \in \mathbb{N} \mid \exists G \text{, } G \text{ is an} (x, y)\text{-graph } \land \text{ } |V(G)| = n \}
    $$
\end{definition}

\begin{lemma}[Graver's Lemma 1]
    \label{l:Lemma1}
    \lean{SimpleGraph.Lemma₁}
    \leanok
    \uses{def:xyGraph}
    $G$ is an $(x,y)$-graph if and only if its complement $G^c$ is a $(y,x)$-graph.
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{theorem}[noXYGraphIffRamseyGraphProp]
    \label{thm:noXYGraphIffRamseyGraphProp}
    \lean{SimpleGraph.noXYGraphIffRamseyGraphProp}
    \leanok
    \uses{def:xyGraph, l:exists_isNClique_of_le_cliqueNum, l:induce_compl}
\end{theorem}
\begin{proof}
    \leanok
\end{proof}

\begin{lemma}[isXYGraph\_bddAbove]
    \label{l:isXYGraph_bddAbove}
    \lean{SimpleGraph.isXYGraph_bddAbove}
    \leanok
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{lemma}[cardLERamseyOld]
    \label{l:cardLERamseyOld}
    \lean{SimpleGraph.cardLERamseyOld}
    \leanok
    \uses{def:xyGraph, l:isXYGraph_bddAbove, l:cliqueNum, l:indepNum}
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{lemma}[R3y\_neighbor\_Ind]
    \label{l:R3y_neighbor_Ind}
    \lean{SimpleGraph.R3y_neighbor_Ind}
    \leanok
    \uses{def:xyGraph, l:cliqueNum_lt_iff_cliqueFree}
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{theorem}[GraphRamsey2RamseyOld]
    \label{thm:GraphRamsey2RamseyOld}
    \lean{SimpleGraph.GraphRamsey2RamseyOld}
    \leanok
    \uses{def:xyGraph, def:ramseynumber, thm:noXYGraphIffRamseyGraphProp, l:cliqueNum, l:indepNum, l:isXYGraph_bddAbove}
\end{theorem}
\begin{proof}
    \leanok
\end{proof}

\begin{theorem}[RamseyOld\_2]
    \label{thm:RamseyOld₂}
    \lean{SimpleGraph.RamseyOld₂}
    \leanok
    \uses{def:ramseynumber, thm:GraphRamsey2RamseyOld}
\end{theorem}
\begin{proof}
    \leanok
\end{proof}

\begin{definition}[$σ(G)$]
    \label{def:σ_G}
    \lean{SimpleGraph.σ_G}
    \leanok
    \uses{def:ramseynumber}
\end{definition}

\begin{lemma}[sum\_degree\_eq\_sum\_over\_degrees]
    \label{l:sum_degree_eq_sum_over_degrees}
    \lean{SimpleGraph.sum_degree_eq_sum_over_degrees}
    \leanok
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{lemma}[num\_of\_vertices\_eq\_sum\_over\_degrees]
    \label{l:num_of_vertices_eq_sum_over_degrees}
    \lean{SimpleGraph.num_of_vertices_eq_sum_over_degrees}
    \leanok
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{definition}[$H_1$]
    \label{def:H₁}
    \lean{SimpleGraph.H₁}
    \leanok
\end{definition}

\begin{definition}[$H_2$]
    \label{def:H₂}
    \lean{SimpleGraph.H₂}
    \leanok
\end{definition}

\begin{definition}[$H_1 H_2$ isomorphic]
    \label{def:H₁₂_iso}
    \lean{SimpleGraph.H₁₂_iso}
    \leanok
    \uses{def:H₁, def:H₂}
\end{definition}

\begin{lemma}[$H_1$\_eq\_bot\_of\_3y]
    \label{l:H₁_eq_bot_of_3y}
    \lean{SimpleGraph.H₁_eq_bot_of_3y}
    \leanok
    \uses{def:H₁, def:xyGraph}
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{lemma}[$H_1$\_cliqueNum\_lt]
    \label{l:H₁_cliqueNum_lt}
    \lean{SimpleGraph.H₁_cliqueNum_lt}
    \leanok
    \uses{def:H₁, l:cliqueNum_pos}
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{theorem}[Graver Lemma 2]
    \label{thm:Lemma₂}
    \lean{SimpleGraph.Lemma₂}
    \leanok
    \uses{def:xyGraph, def:ramseynumber, def:H₁, l:indepNum_mono, l:H₁_cliqueNum_lt, l:cliqueNum_pos, l:indepNum_pos}
    For any $(x,y,n)$-graph $G$, every vertex $v$ in $G$ satisfies 
    \[(n - 1) - R(x,y - 1) \leq deg(v) \leq R(x - 1, y)\].
\end{theorem}
\begin{proof}
    \leanok
\end{proof}

\begin{lemma}[G\_degreeCount\_eq]
    \label{l:G_degreeCount_eq}
    \lean{SimpleGraph.G_degreeCount_eq}
    \leanok
    \uses{def:xyGraph, def:ramseynumber, def:σ_G, l:sum_degree_eq_sum_over_degrees, thm:Lemma₂}
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{lemma}[G\_vertCount\_eq]
    \label{l:G_vertCount_eq}
    \lean{SimpleGraph.G_vertCount_eq}
    \leanok
    \uses{def:xyGraph, def:ramseynumber, def:σ_G, l:num_of_vertices_eq_sum_over_degrees, thm:Lemma₂}
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{lemma}[$H_1$\_degreeCount\_eq]
    \label{l:H₁_degreeCount_eq}
    \lean{SimpleGraph.H₁_degreeCount_eq}
    \leanok
    \uses{def:xyGraph, def:ramseynumber, def:σ_G, thm:Lemma₂}
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{lemma}[$H_1$\_vertCount\_eq]
    \label{l:H₁_vertCount_eq}
    \lean{SimpleGraph.H₁_vertCount_eq}
    \leanok
    \uses{def:xyGraph, def:ramseynumber, def:σ_G, thm:Lemma₂}
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{theorem}[Graver's Prop 1]
    \label{thm:Prop₁}
    \lean{SimpleGraph.Prop₁}
    \leanok
    \uses{def:xyGraph, def:ramseynumber, def:σ_G, thm:Lemma₂}
\end{theorem}
\begin{proof}
    \leanok
\end{proof}

\begin{lemma}[$v_i$\_deg\_swap]
    \label{l:vᵢ_deg_swap}
    \lean{SimpleGraph.vᵢ_deg_swap}
    \leanok
    \uses{def:xyGraph, def:σ_G, thm:Lemma₂}
\end{lemma}
\begin{proof}
    \leanok
\end{proof}

\begin{theorem}[Graver's Prop 2]
    \label{thm:Prop₂}
    \lean{SimpleGraph.Prop₂}
    \leanok
    \uses{def:xyGraph, def:ramseynumber, def:σ_G, def:H₁, def:H₂, def:toOrderedPair, l:toOrderedPair_repr, l:exists_mem_pair, l:toOrderedPair_inj, l:toOrderedPair_IsDiag_of_eq}
\end{theorem}
\begin{proof}
    \leanok
\end{proof}